function calCartPrice(val1,val2,...num1){
    return num1
   
    }
    console.log(calCartPrice(200,500,700,800,400))

    const user={
        "name" : "rituraj" ,
        "lastName" : "pal"

    }

    function handleObject(anyObject){
        console.log(`user name is ${anyObject.name} and lastName is ${anyObject.lastName}`)

    }
    handleObject(user)

    const newArray=[200,600,1200,70]
    
    function returnArray (getArray){
    return getArray[2]
    }
    console.log(returnArray(newArray))